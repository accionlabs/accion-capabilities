import React from 'react';
import EntityPage from './EntityPage';

const PlatformsPage: React.FC = () => {
  return (
    <EntityPage
      entityType="platform"
      title="Platforms"
      description="Enterprise-grade platforms that accelerate digital transformation and enable innovation at scale"
    />
  );
};

export default PlatformsPage;